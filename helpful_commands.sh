# comments
# `$` - represents the exit status of the previous command. Exit status as `0` if successful, else `1` for unsuccessful.
# commonly used commands
# working with files and directories
# managing data
# file I/O handling
